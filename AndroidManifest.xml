<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="strength.history"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="strength.history.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="strength.history.settings.ExerciseActivity" >
        </activity>

        <service
            android:name="strength.history.data.service.local.LocalExerciseService"
            android:exported="false" />
        <service
            android:name="strength.history.data.service.local.LocalWeightService"
            android:exported="false" />
        <service
            android:name="strength.history.data.service.local.LocalWorkoutService"
            android:exported="false" />
        <service
            android:name="strength.history.data.service.local.LocalWorkoutDataService"
            android:exported="false" />

        <activity
            android:name="strength.history.ExercisesActivity"
            android:label="@string/title_activity_exercises" >
        </activity>
    </application>

</manifest>
